/// App runtime defaults (non-secret).
///
/// This is the public Droppy site origin (upload page), not the `/admin` panel.
abstract final class AppConfigDefaults {
  AppConfigDefaults._();

  static const String apiBaseUrl = 'https://sharelargefilesfree.com/';
}

